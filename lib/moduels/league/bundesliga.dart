import 'package:flutter/material.dart';

class BundesLiga extends StatelessWidget{
  const BundesLiga({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('BundesLiga');
  }
}