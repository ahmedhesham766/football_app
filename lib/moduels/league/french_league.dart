import 'package:flutter/material.dart';

class FrenchLeague extends StatelessWidget{
  const FrenchLeague({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('FrenchLeague');
  }
}